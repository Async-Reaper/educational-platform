import { AuthData } from 'features/auth/model/types';
import { AppDispatch } from 'app/store';

export const auth = (data: AuthData) => (dispatch: AppDispatch) => {
  try {

  } catch (e) {
    console.log(e);
  }
};
